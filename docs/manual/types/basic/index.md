# 基本型
基本型には `Bool`、`Float`、`Int` があります。これらは、構文上、次のような値によって容易に識別できます↓

- `Bool` の場合は `true` および `false`。
- `Int` の場合は `1`、`0`、`-1` および `0xFF0000`。
- `Float` の場合は `1.0`、`0.0`、`-1.0`、`1e10`。

Haxeにおいて、基本型はクラスではありません。その代わりに、これらは抽象型として実装されており、後述するセクションで説明するように、コンパイラの内部的な演算子処理と密接に関連しています。