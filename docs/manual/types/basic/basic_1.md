# 数値型
!!! info "Define: Float"
    _<!--IEEE 64ビット倍精度浮動-->小数点数を表す。_
!!! info "Define: Int"
    _整数を表す。_

すべての `Int` は `Float` が期待される場所で使用できます(つまり、**`Int` は `Float` に代入可能**であり、**`Float` と統一されます**)が、その逆は成り立ちません。<br>
`Float` を `Int` に代入すると、精度が失われる可能性があるため、暗黙的な代入は許可されていません。
